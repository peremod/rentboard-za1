# Deploys the Angular SSR frontend to Vercel on push to main (production)
# or develop (staging) — matches CONTRIBUTING.md §8 environment-branch mapping.
name: Deploy Frontend

on:
  push:
    branches: [master, main, develop]
    paths: ['frontend/**']

jobs:
  deploy:
    name: Deploy to Vercel
    runs-on: ubuntu-latest
    # 'master' and 'main' both map to production so the branch rename in
    # CONTRIBUTING.md §8 can happen without a deploy gap.
    environment: ${{ (github.ref_name == 'main' || github.ref_name == 'master') && 'production' || 'staging' }}
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install Vercel CLI
        run: npm install -g vercel@latest

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Build (production config on main, staging config on develop)
        working-directory: frontend
        run: npm run ${{ (github.ref_name == 'main' || github.ref_name == 'master') && 'build:prod' || 'build:staging' }}

      - name: Deploy to Vercel
        working-directory: frontend
        run: vercel deploy --prebuilt ${{ (github.ref_name == 'main' || github.ref_name == 'master') && '--prod' || '' }} --token=${{ secrets.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
