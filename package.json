{
  "name": "rentboard-za",
  "version": "1.2.5",
  "private": true,
  "description": "RentBoard ZA \u2014 South Africa's room-letting notice board. Monorepo root (git hooks + workspace scripts only; app code lives in frontend/ and backend/).",
  "scripts": {
    "prepare": "husky",
    "frontend": "npm --prefix frontend run start",
    "backend": "npm --prefix backend run start:dev",
    "install:all": "npm install && npm --prefix frontend install && npm --prefix backend install"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.6"
  }
}
