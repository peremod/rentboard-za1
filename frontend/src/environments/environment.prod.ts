/** Production — `ng build --configuration production`, deployed to rentboard.co.za */
export const environment = {
  production: true,
  envName: 'production',
  apiUrl: 'https://api.rentboard.co.za/api',
  imagekitUrl: 'https://ik.imagekit.io/rentboard',
};
